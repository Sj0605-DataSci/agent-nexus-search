[build]
builder = "DOCKERFILE"
dockerfilePath = "fastapi_backend/Dockerfile"

[deploy]
startCommand = "uvicorn app.main:app --port 8000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1

[variables]
PORT = "8000"